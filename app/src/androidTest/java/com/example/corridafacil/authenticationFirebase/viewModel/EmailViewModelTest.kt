package com.example.corridafacil.authenticationFirebase.viewModel

import androidx.test.internal.runner.junit4.AndroidJUnit4ClassRunner

import org.junit.runner.RunWith

@RunWith(AndroidJUnit4ClassRunner::class)
class EmailViewModelTest{
/*
    @MockK
    private lateinit var mockEmailRepositoryImpl: EmailRepositoryImpl

    private lateinit var viewModel:EmailViewModel

    @get:Rule
    val instantTaskExecutorRule = InstantTaskExecutorRule()

    @Before
    fun setUp(){
        mockEmailRepositoryImpl = mockk(relaxed = true)
        viewModel = EmailViewModel(mockEmailRepositoryImpl)
    }

    @Test
    fun checando_dipositivo_do_usuario() = runBlocking {
        val result = viewModel.checkUserDevice()
        assertEquals(true, result)

    }

    @Test
    fun verificar_se_token_sao_iguais() = runBlocking{

        val mockEmailRepository = mockk<EmailRepository>( relaxed = true)
        val mockPassageiroDAO = mockk<PassageiroDAO>( relaxed = true)

        val uidUserAuth = mockEmailRepository.userAuthenticated()

        val passageiro = mockPassageiroDAO.readDataUser(uidUserAuth!!.uid)

        Log.i("Token passageiro atual", "Token: "+passageiro.tokenPassageiro)

    }

 */


}